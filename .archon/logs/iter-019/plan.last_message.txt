Plan phase complete.

Processed the Doob-Meyer prover result, archived it, and cleared `.archon/task_results/`. Updated the strategy and pending state so iteration 019 now targets `BrownianMotion/StochasticIntegral/QuadraticVariationBrownian.lean`.

The next prover objective is:

```lean
ProbabilityTheory.quadraticVariation_brownian (t : ℝ≥0) :
  brownianQuadraticVariation t =ᵐ[gaussianLimit] fun _ => (t : ℝ)
```

I also updated `blueprint/src/chapters/stochastic_integral.tex` with the deterministic time-process helper sketches and the fixed-time a.e Brownian QV proof route. No Lean files were edited, so I did not rerun `lake build`; the incoming prover report already had a passing full build.