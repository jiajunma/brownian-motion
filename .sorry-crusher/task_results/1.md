# Session 1 prover results

## Status: IN_PROGRESS — Layers A, B complete and compiling; C/D/E remain.

### Done (committed)
- ad8979c Layer A: hittingBtwn/crossing congruence, `mul_upcrossingsBefore_le_sum_add_max`
  (pathwise upcrossing ineq with (a − f N)⁺ correction via endpoint-max trick),
  `le_upcrossingsBefore_of_alternating` (via `upcrossingsBefore_lt_of_exists_upcrossing`).
- Layer B: `pullbackFiltration`, `finIdx` (+ mono/lt_iff/mem/zero_eq_bot/exists lemmas),
  `exists_elementaryPredictableSet_integral_eq` (THE bridge: 0/1 adapted weights ↦
  ElementaryPredictableSet, integral = Σ W·ΔX; dite-choose construction, pairwise disjoint via
  consecutive Ioc),
  `mul_integral_upcrossingsBefore_finIdx_le` (E-bound: (b−a)∫U ≤ C + ∫(a−Xt)⁺),
  `altSet` + mono + `altSet_subset_upcrossingsBefore` + `measureReal_altSet_le` (≤ K/(b−a)/m),
  `sum_indicator_mul_sub_eq_stopped` (A4 telescoping),
  `integral_sum_weight_increments_mem_Icc` (two-sided via 1−W complement),
  `mul_measureReal_exists_lt_le` (one-sided maximal core),
  `measureReal_exists_abs_lt_le` (maximal: μ{∃s∈F, lam<|Xs|} ≤ 2K′/lam).

### Key insights (for future sessions)
- `Monotone.measure_iUnion`: continuity from below holds for ARBITRARY monotone set sequences
  (no measurability needed) — use it for the C-layer countable unions.
- altSet/maximal-event monotone in F; alternation lemma replaces refinement-monotonicity entirely.
- `lt_abs`, `measureReal_union_le`, `setIntegral_le_integral`, `abs_integral_le_integral_abs`,
  `hittingBtwn_eq_hittingBtwn_of_exists`, `hittingBtwn_lt_iff` all exist and were used.
- Gotchas hit: rw under binders (use simp only with ∀-family eq), dite needs beta (simp only
  [dif_pos h]), motive issues (rewrite at hypothesis instead), set-bound vars need explicit
  `have h' : idx k = s := hkeq` defeq coercions, `Set.indicator_of_(not)Mem` needs the
  membership proof typed explicitly first.

### Next (Layer C/D/E plan in PROGRESS.md)
- C: for countable T' ⊆ ι, t ∈ D-cofinal: μ(⋃_j altSet F_j) ≤ ofReal(K/m) via exhaustion
  F_j := ((range j).image σ).filter (· ≤ t) ∪ guard; ⋂_m has measure 0; same for maximal events;
  define good set G(T') (full measure) where: all rational a<b have finitely many alternations
  along T'∩Iic d ∀ d ∈ D, and sup |X| < ∞ on T'∩Iic d.
- D: pure path analysis: on G, one-sided limits along 𝓟 T' exist everywhere
  (tendsto_of_no_upcrossings with s := range ℚ-cast); regularization r(x) := right-lim;
  r right-continuous + left limits.
- E: needs [TopologicalSpace.SeparableSpace ι] ADDED to the statement (mathematically
  necessary). S-countability via separable+DenselyOrdered disjoint-(p,b_p) argument and the
  D∪{tₖ}∪{t*} rerun; final Y via G″ := G(D ∪ S) patching.
